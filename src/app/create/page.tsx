"use client"
import { Flex } from '@chakra-ui/react';
import React from 'react';

type pageProps = {
    
};

const page:React.FC<pageProps> = () => {
    
    return (
        <Flex>
            hi
        </Flex>
    )
}
export default page;